{{!< default}}
{{! > alertMsgMoreInfoBody}}


<nav>
    <ul>
    {{#each splash}}
        <li>
            <a href="{{url}}">{{id}}</a>

        </li>
    {{/each}}
    </ul>
</nav>


{{#each splash}}
    <section id="{{id}}" class="splash">
        <header><h1>{{title}}</h1></header>

    </section>
{{/each}}


<section>
    {{> splash_d7_v1}}
</section>














